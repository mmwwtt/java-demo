package com.mmwwtt.demo.ee.validation;

import org.springframework.context.annotation.Configuration;

@Configuration
public class ValidationUtils {

//    // 方式一
//    @Bean("validator")
//    public static Validator buildDefaultValidatorFactory() {
////        return Validation.buildDefaultValidatorFactory().getValidator();
//////        validator.validate(baseInfoCreateDTO);
////
////        Set<ConstraintViolation<Object>> constraintViolations = validator
////                .validate(baseInfoCreateDTO);//验证某个对象,，其实也可以只验证其中的某一个属性的
////        Iterator<ConstraintViolation<Object>> iter = constraintViolations
////                .iterator();
////        List<String> error = new ArrayList<>();
////        while (iter.hasNext()) {
////            String message = iter.next().getMessage();
////            error.add(message);
////        }
////        throw
//    }

}
